{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
    "rewrites": [
  { "source": "/(.*)", "destination": "/index.html" }
]
    }
  ]
}